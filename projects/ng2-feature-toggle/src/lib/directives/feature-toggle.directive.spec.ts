import { FeatureToggleDirective } from './feature-toggle.directive';

xdescribe('FeatureToggleDirective', () => {
  it('should create an instance', () => {
    // const directive = new FeatureToggleDirective();
    // expect(directive).toBeTruthy();
  });
});
